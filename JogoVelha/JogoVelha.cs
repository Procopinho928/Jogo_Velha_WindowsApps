public class Velha
{
    public bool[] status = new bool[9];
    public void Analisar(int posicao)
    {
        status[posicao] = true;

        if (status[posicao] == true)//OCUPADO
        {
            
        }
        
    }
}